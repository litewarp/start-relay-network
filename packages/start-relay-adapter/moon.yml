language: typescript
layer: "library"
stack: "frontend"
tags:
  - "vitest"

tasks:
  build:
    command: tsdown
    deps:
      - typecheck
      - lint

  dev:
    command: tsdown --watch
    options:
      persistent: true
      cache: false
