# Postgres connection for the embedded PostGraphile handler at /api/graphql.
# CONNECTION_STRING wins over DATABASE_URL; both default to the local Docker
# database started by `moon run api:db-up` (see apps/api/.env.example).
CONNECTION_STRING=postgres://postgres:postgres@localhost:6432/starwars
